<Window x:Class="FilesToClipboard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FilesToClipboard"
        Title="File Content Collector"
        Width="960" MinHeight="600"
        SizeToContent="Height"
        WindowStartupLocation="Manual"
        Left="100" Top="100"
        Topmost="True"
        Background="{DynamicResource Brush.Surface}"
        Foreground="{DynamicResource Brush.Text}"
        FontFamily="Segoe UI Variable, Segoe UI"
        FontSize="13.5"
        AllowDrop="True"
        DragOver="Window_DragOver"
        Drop="Window_Drop">

    <Window.Resources>
        <local:FileNameConverter x:Key="FileNameConverter" />
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Dark palette -->
        <SolidColorBrush x:Key="Brush.Surface"        Color="#0F0F10"/>
        <SolidColorBrush x:Key="Brush.Card"           Color="#17171A"/>
        <SolidColorBrush x:Key="Brush.CardBorder"     Color="#26262A"/>
        <SolidColorBrush x:Key="Brush.Text"           Color="#F5F5F7"/>
        <SolidColorBrush x:Key="Brush.TextSecondary"  Color="#B9BBC1"/>
        <SolidColorBrush x:Key="Brush.Input"          Color="#1C1C20"/>
        <SolidColorBrush x:Key="Brush.Accent"         Color="#5B9BFF"/>
        <SolidColorBrush x:Key="Brush.AccentHover"    Color="#4A84DF"/>
        <SolidColorBrush x:Key="Brush.Success"        Color="#2E7D32"/>
        <SolidColorBrush x:Key="Brush.Danger"         Color="#C62828"/>

        <!-- Slight tint for unresolved tabs -->
        <SolidColorBrush x:Key="Brush.TabUnresolvedBackground" Color="#1A5B9BFF"/>

        <!-- Button base -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text}"/>
            <Setter Property="Background" Value="{DynamicResource Brush.Card}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Brush.CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="6,0,6,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource Brush.Accent}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="b" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Accent and Danger variants -->
        <Style x:Key="AccentButton" BasedOn="{StaticResource ModernButton}" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Brush.Accent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="8"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{DynamicResource Brush.AccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="b" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" BasedOn="{StaticResource ModernButton}" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Brush.Danger}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{x:Null}"/>
        </Style>

        <!-- TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text}"/>
            <Setter Property="Background" Value="{DynamicResource Brush.Input}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Brush.CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="bd"
                                CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource Brush.Accent}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBox "card" -->
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource Brush.Card}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Brush.CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Focusable="False" Padding="{TemplateBinding Padding}">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Item container with rounded hover/selection -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="bd" CornerRadius="8" Background="Transparent">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#1FFFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#23FFFFFF"/>
                                <Setter Property="Foreground" Value="{DynamicResource Brush.Text}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TOP-LEVEL TabControl with "+" button -->
        <Style x:Key="TopTabControlStyle" TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                                <TabPanel x:Name="HeaderPanel"
                                          Panel.ZIndex="1"
                                          IsItemsHost="True"
                                          Margin="0,0,4,0" />
                                <Button Content="+"
                                        Width="28"
                                        Height="24"
                                        Margin="0,0,0,0"
                                        Padding="0"
                                        Style="{StaticResource ModernButton}"
                                        Click="AddTabButton_Click"/>
                            </StackPanel>
                            <Border BorderBrush="{DynamicResource Brush.CardBorder}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Background="{DynamicResource Brush.Card}"
                                    Margin="0,6,0,0">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                  Margin="0"
                                                  ContentSource="SelectedContent" />
                            </Border>
                        </DockPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- NESTED (Suffix) TabControl with its own "+" button -->
        <Style x:Key="SuffixTabControlStyle" TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                                <TabPanel x:Name="HeaderPanel"
                                          Panel.ZIndex="1"
                                          IsItemsHost="True"
                                          Margin="0,0,4,0" />
                                <Button Content="+"
                                        Width="28"
                                        Height="24"
                                        Margin="0,0,0,0"
                                        Padding="0"
                                        Style="{StaticResource ModernButton}"
                                        Click="AddSuffixTabButton_Click"/>
                            </StackPanel>

                            <!-- No extra card/border here; the surrounding "Suffix" Border already is a card -->
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              Margin="0,6,0,0"
                                              ContentSource="SelectedContent" />
                        </DockPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TabItem style: underline color + hover close -->
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text}" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="RootBorder"
                                CornerRadius="8,8,0,0"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True">
                            <Border BorderThickness="0,0,0,2"
                                    BorderBrush="{Binding UnderlineBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding Header}"
                                               Margin="10,2,4,2"
                                               TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Center" />
                                    <Button x:Name="CloseButton"
                                            Grid.Column="1"
                                            Width="18"
                                            Height="18"
                                            Margin="0,0,6,0"
                                            Padding="0"
                                            Style="{StaticResource ModernButton}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            ToolTip="Close tab"
                                            Visibility="Collapsed"
                                            Click="TabCloseButton_Click">
                                        <TextBlock Text="&#xE711;"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   FontSize="10" />
                                    </Button>
                                </Grid>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CloseButton" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Brush.Card}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6" />
                            </Trigger>

                            <!-- resolved/unresolved background -->
                            <DataTrigger Binding="{Binding IsResolved}" Value="False">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Brush.TabUnresolvedBackground}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsResolved}" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="Transparent" />
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <TabControl x:Name="tabControl"
                    Margin="14"
                    Style="{StaticResource TopTabControlStyle}"
                    ItemsSource="{Binding Tabs}">
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Title + Command bar -->
                        <Border Grid.Row="0"
                                CornerRadius="12"
                                Background="{DynamicResource Brush.Card}"
                                BorderBrush="{DynamicResource Brush.CardBorder}"
                                BorderThickness="1"
                                Padding="10"
                                Margin="0,0,0,10">
                            <DockPanel LastChildFill="False">
                                <StackPanel DockPanel.Dock="Left"
                                            Orientation="Vertical"
                                            Margin="2,0,16,0">
                                    <TextBlock Text="File Content Collector"
                                               FontSize="18"
                                               FontWeight="SemiBold"/>
                                    <TextBlock Text="Collect, preview, and export concatenated file contents"
                                               Foreground="{DynamicResource Brush.TextSecondary}"
                                               FontSize="12"/>
                                </StackPanel>

                                <!-- Unresolve visibility is based on ACTIVE SECOND-LEVEL TAB -->
                                <Button DockPanel.Dock="Right"
                                        Style="{StaticResource ModernButton}"
                                        Margin="6,0,0,0"
                                        Visibility="{Binding ActiveSuffixTab.IsResolved, Converter={StaticResource BoolToVis}}"
                                        Click="btnUnresolve_Click">
                                    <StackPanel Orientation="Horizontal" Margin="10,0">
                                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE777;" Margin="0,0,6,0"/>
                                        <TextBlock Text="Unresolve"/>
                                    </StackPanel>
                                </Button>

                                <!-- Primary action -->
                                <Button DockPanel.Dock="Right"
                                        Style="{StaticResource AccentButton}"
                                        Margin="6,0,0,0"
                                        ToolTip="Copy all selected files' contents with prefix/suffix"
                                        Click="btnCopy_Click">
                                    <Button.Content>
                                        <StackPanel Orientation="Horizontal" Margin="10,0">
                                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8C8;" Margin="0,0,6,0"/>
                                            <TextBlock Text="Copy"/>
                                        </StackPanel>
                                    </Button.Content>
                                </Button>

                                <!-- Secondary actions -->
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource ModernButton}"
                                            Click="btnSelectFiles_Click"
                                            ToolTip="Add files">
                                        <StackPanel Orientation="Horizontal" Margin="10,0">
                                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8E5;" Margin="0,0,6,0"/>
                                            <TextBlock Text="Files"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource ModernButton}"
                                            Click="btnSelectDirs_Click"
                                            ToolTip="Add directories (recursive)">
                                        <StackPanel Orientation="Horizontal" Margin="10,0">
                                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8B7;" Margin="0,0,6,0"/>
                                            <TextBlock Text="Folders"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource ModernButton}"
                                            Click="btnPastePath_Click"
                                            ToolTip="Paste path(s) from clipboard">
                                        <StackPanel Orientation="Horizontal" Margin="10,0">
                                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE77F;" Margin="0,0,6,0"/>
                                            <TextBlock Text="Paste Path"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource DangerButton}"
                                            Click="btnClear_Click"
                                            ToolTip="Clear list">
                                        <StackPanel Orientation="Horizontal" Margin="10,0">
                                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE74D;" Margin="0,0,6,0"/>
                                            <TextBlock Text="Clear"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource ModernButton}"
                                            Click="btnClearTexts_Click"
                                            ToolTip="Clear prefix + active suffix">
                                        <StackPanel Orientation="Horizontal" Margin="10,0">
                                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE894;" Margin="0,0,6,0"/>
                                            <TextBlock Text="Clear Texts"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </DockPanel>
                        </Border>

                        <!-- Prefix -->
                        <Border Grid.Row="1"
                                CornerRadius="10"
                                Background="{DynamicResource Brush.Card}"
                                BorderBrush="{DynamicResource Brush.CardBorder}"
                                BorderThickness="1"
                                Padding="8"
                                Margin="0,0,0,10">
                            <DockPanel>
                                <TextBlock DockPanel.Dock="Left"
                                           Text="Prefix"
                                           Width="60"
                                           VerticalAlignment="Top"
                                           Margin="0,4,8,0"
                                           Foreground="{DynamicResource Brush.TextSecondary}"/>
                                <TextBox Text="{Binding PrefixText, UpdateSourceTrigger=PropertyChanged}"
                                         TextWrapping="Wrap"
                                         AcceptsReturn="True"
                                         VerticalScrollBarVisibility="Auto"
                                         MinHeight="52"
                                         MaxHeight="250"
                                         GotKeyboardFocus="TextBox_GotKeyboardFocus"
                                         SelectionChanged="TextBox_SelectionChanged"/>
                            </DockPanel>
                        </Border>

                        <!-- Files list -->
                        <Grid Grid.Row="2" Margin="0,0,0,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="Selected files"
                                       Foreground="{DynamicResource Brush.TextSecondary}"
                                       Margin="2,0,0,6"/>
                            <ListBox x:Name="lstSelectedPaths"
                                     Grid.Row="1"
                                     ItemsSource="{Binding SelectedPaths}"
                                     SelectionMode="Extended"
                                     PreviewMouseLeftButtonDown="lstSelectedPaths_PreviewMouseLeftButtonDown"
                                     MouseMove="lstSelectedPaths_MouseMove"
                                     DragOver="lstSelectedPaths_DragOver"
                                     Drop="lstSelectedPaths_Drop"
                                     AllowDrop="True"
                                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto">

                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid HorizontalAlignment="Stretch">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Remove -->
                                            <Button Grid.Column="0"
                                                    Style="{StaticResource ModernButton}"
                                                    Padding="8,4"
                                                    Margin="0,0,10,0"
                                                    Click="btnRemovePath_Click"
                                                    ToolTip="Remove">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE711;"/>
                                            </Button>

                                            <!-- Name + path -->
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding Converter={StaticResource FileNameConverter}}"
                                                           Foreground="{DynamicResource Brush.Text}"
                                                           FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding}"
                                                           Foreground="{DynamicResource Brush.TextSecondary}"
                                                           FontSize="12"/>
                                            </StackPanel>

                                            <!-- Insert file name at caret (fix: remember focus BEFORE click) -->
                                            <Button Grid.Column="2"
                                                    Style="{StaticResource ModernButton}"
                                                    Padding="8,4"
                                                    Margin="10,0,10,0"
                                                    Click="btnInsertName_Click"
                                                    PreviewMouseLeftButtonDown="InsertNameButton_PreviewMouseLeftButtonDown"
                                                    ToolTip="Insert file name at caret">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8A5;"/>
                                            </Button>

                                            <!-- Drag handle icon (visual only) -->
                                            <TextBlock Grid.Column="3"
                                                       FontFamily="Segoe MDL2 Assets"
                                                       Text="&#xE76F;"
                                                       Margin="10,0,0,0"
                                                       VerticalAlignment="Center"
                                                       Opacity="0.6"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>

                        <!-- Suffix (nested tab control) -->
                        <Border Grid.Row="3"
                                CornerRadius="10"
                                Background="{DynamicResource Brush.Card}"
                                BorderBrush="{DynamicResource Brush.CardBorder}"
                                BorderThickness="1"
                                Padding="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="Suffix"
                                           VerticalAlignment="Top"
                                           Margin="0,4,8,0"
                                           Foreground="{DynamicResource Brush.TextSecondary}"/>

                                <TabControl Grid.Column="1"
                                            Style="{StaticResource SuffixTabControlStyle}"
                                            ItemsSource="{Binding SuffixTabs}"
                                            SelectedItem="{Binding ActiveSuffixTab, Mode=TwoWay}">
                                    <TabControl.ContentTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding SuffixText, UpdateSourceTrigger=PropertyChanged}"
                                                     TextWrapping="Wrap"
                                                     AcceptsReturn="True"
                                                     VerticalScrollBarVisibility="Auto"
                                                     MinHeight="52"
                                                     MaxHeight="250"
                                                     GotKeyboardFocus="TextBox_GotKeyboardFocus"
                                                     SelectionChanged="TextBox_SelectionChanged"/>
                                        </DataTemplate>
                                    </TabControl.ContentTemplate>
                                </TabControl>
                            </Grid>
                        </Border>
                    </Grid>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</Window>
